namespace HiddenGold.Stones
{
    // What a stone hides inside. Visuals must stay identical across all
    // three types so the player can't tell them apart before tapping.
    public enum StoneType
    {
        Empty,
        Gold,
        Trap
    }
}
