# تطهير DOM/CSS → PixiJS خالص — سجل التقدم

آخر تحديث: النافذة ٣ (`TextComposer.ts` — الشريط العلوي وشريط التأثيرات)
**مكتملة بالكامل**، بما في ذلك حقل إدخال النص نفسه — لم يعد هناك عنصر
`<input>` مرئي أو دائم في التطبيق إطلاقًا. راجع قسم "بروتوكول الجسر
المخفي" أدناه لتفاصيل كيف يعمل استقبال الكيبورد الآن.

## الهدف
حذف `src/ui/mazajUI.css` وكل عنصر DOM يمكن تحويله فعليًا إلى كائنات PixiJS
(`Container`/`Graphics`/`Sprite`/`Text`) في منطقة عمل التطبيق، بنفس معايير
الدقة المطبّقة على صندوق تحكم النص: هندسة مُقاسة من DOM حقيقي (لا تخمين)،
مناطق لمس 44×44px صريحة، تحقق بصري بلقطات شاشة فعلية، واختبار عدم تسرب
اللمسات لمستمع "إطلاق الصاروخ" في `app.stage`.

## قيد تقني حقيقي واحد متبقٍ — لن يختفي مهما تحوّل الباقي
ليس تهربًا، بل استحالة تقنية موضوعية:

**منتقي الملفات الأصلي من نظام التشغيل** (`#mzj-bg-image`/`#mzj-bg-video`
في `PlanningScreen.ts`) — نافذة اختيار الملف/الفيديو من الجهاز نافذة نظام
تشغيل (WebView chrome)، لا يمكن لأي كانفاس/WebGL استدعاءها إلا عبر عنصر
`<input type="file">` حقيقي (حتى لو مخفيًا بصريًا). هذا قيد المتصفح نفسه،
لا علاقة له بـPixi.

## بروتوكول الجسر المخفي (Ghost Bridge) — حقل إدخال النص لم يعد استثناءً دائمًا
كان الافتراض السابق أن حقل `<input>` الحقيقي المرئي استثناء دائم لا مفر
منه. طالب المستخدم صراحةً بإزالته حتى كعنصر مرئي، مع الاعتراف المتبادل بأن
فتح لوحة مفاتيح Android/iOS يتطلب تقنيًا عنصر DOM حقيقي قابل للتركيز (لا
يوجد أي API يمنح الـCanvas تركيز IME). الحل المتفق عليه: **جسر شبح غير
مرئي بالكامل**، وليس حقل إدخال متخفٍ:
- **Shadow DOM معزول**: كل جسر يُبنى داخل `host.attachShadow({mode:'open'})`
  خاص به — حتى لو وُجد خطأ تنسيق، حدود الـShadow تمنع أي تسرب لبقية التطبيق.
- **صفر CSS**: كل خاصية تُضبط عبر `style.*` مباشرة من TypeScript — لا صف
  واحد في `mazajUI.css` يخص هذا العنصر.
- **شبح حقيقي**: 1×1px، `opacity:0`، خارج حدود الشاشة تمامًا
  (`left:-9999px; top:-9999px`) — المستخدم لا يرى أي بكسل منه إطلاقًا.
- **بناء عند التركيز، تدمير فوري عند `blur`**: `openGhostInput()` يبني
  الجسر من الصفر عند الحاجة للكتابة، و`closeGhostInput()` يزيله بالكامل
  من الـDOM لحظة فقدان التركيز — لا بقايا ميتة أبدًا بين جلسات الكتابة.
- **منطق استقبال الحروف الفعلي**: مستمع `input` واحد على الجسر ينسخ
  `ghost.value` إلى `this.text` في كل ضغطة/IME-commit/لصق، ثم يعيد رسم
  الحروف عبر `refreshInputVisual()` — كل بكسل مرئي (النص، placeholder،
  المؤشر الوامض) كائن `PIXI.Text`/`PIXI.Graphics` حقيقي، لا الجسر نفسه
  أبدًا. المؤشر: مستطيل ذهبي 2×20px، `AdvancedBloomFilter`+
  `DropShadowFilter` حقيقيان، يومض عبر `setInterval` (530ms، نفس معدل
  Chrome/Android الافتراضي)، ويتتبّع طول النص بدقة (`-text.width/2` من
  المركز، لأن العربية RTL فالحرف الجديد يُضاف يسار الكتلة الحالية).

كل الكود في `TextComposer.ts::openGhostInput()`/`closeGhostInput()`/
`refreshInputVisual()`/`buildInputField()`.

## الجرد الكامل

| # | الموقع | العناصر | الحالة |
|---|--------|---------|--------|
| 1 | `HeaderBar.ts` | 4 أزرار (رئيسية/ابدأ العرض/FPS/كتم) | ✅ منجز — Pixi خالص |
| 2 | `PlanningIconColumn.ts` | 14 زر أيقونة | ✅ منجز — Pixi خالص، صفر DOM |
| 3 | `PlanningSubpanels.ts` — لوحة الكاميرا | زرا اختيار (رفع/توثيق مباشر) | ✅ منجز — Pixi خالص |
| 4 | `PlanningSubpanels.ts` + `PixiSlider.ts` — الإضاءة/التوهج/المختبر | 6 أشرطة تمرير | ✅ منجز — عنصر Slider جديد بالكامل، مبني من الصفر |
| 5 | `UploadHint.ts` | تنبيه Toast عائم | ⚠️ الـToast نفسه بقي HTML (نافذة قادمة)، لكن **خلل حقيقي اكتُشف وأُصلح**: كان يستهدف `#mzj-glow` DOM المحذوف، فأصبحت ميزة "تمييز شريط التوهج بعد الرفع" معطّلة بصمت. أُصلحت عبر `BottomSheetPanel.pulse()` — تحقق بصري كامل (رفعت صورة تجريبية فعليًا، ولوحة التوهج أضاءت فعلًا) |
| 6 | `TextComposer.ts` — الشريط العلوي + شريط التأثيرات | زر رجوع + حقل إدخال + 7 أطر تأثيرات | ✅ منجز — Pixi خالص بالكامل، **بما فيه حقل الإدخال نفسه** عبر بروتوكول الجسر المخفي (انظر أعلاه) |
| 7 | `#mzj-bg-image` / `#mzj-bg-video` | منتقيا ملفات مخفيان | ⚠️ استثناء دائم (القيد المتبقي الوحيد) |
| 8 | `#mzj-flash` | فلاش الكاميرا الأبيض | ⏳ لم يبدأ (بسيط — تعتيم كامل الشاشة) |

### تفاصيل البند ٢
`src/ui/PlanningIconColumn.ts` — 14 صفًا، هندسة مُقاسة حية
(`ROW_SPACING=58px`، `centerX = screen.width - 56`)، منطقة لمس 44×64px لكل
صف، هالة تسجيل نابضة حقيقية (طبقات شفافية + `blendMode:'add'`)،
`getLeftEdgeX()` ديناميكي لإرساء `ColorPickerPanel`/`ShapesPanel`.

### تفاصيل البند ٣+٤ (منجز الآن)
- **`src/ui/PixiSlider.ts`** (جديد بالكامل — لا يوجد Slider مماثل بالمشروع
  من قبل): مقبض دائري + مسار + تعبئة برتقالية (`#ff9f45`، نفس
  `accent-color` القديم) + قيمة نصية ذهبية (`#ffe9b3`)، سحب عبر
  `pointerdown` على المقبض *أو* المسار (قفزة مباشرة)، تتبّع الحركة عبر
  `app.stage` (نفس أسلوب `PlanningMode.ts`)، منطقة لمس 44×44 للمقبض.
  هندسة مُقاسة حية من صف Slider حقيقي مفتوح (`LABEL_WIDTH=90`,
  `OUTPUT_WIDTH=30`, `GAP=6`، ترتيب RTL: تسمية يمين، مسار وسط، قيمة يسار).
- **`src/ui/BottomSheetPanel.ts`** (جديد — الند الرأسي لـ`SideDockPanel`
  الجانبي): لوحة تنزلق من الأسفل، حواف علوية مدورة فقط (رسم مسار يدوي
  لأن `roundRect` في Pixi لا يدعم زوايا منفصلة)، `pulse()` جديدة تضيء حافة
  اللوحة بحلقة نابضة (بديل الـkeyframe القديم).
- **`src/ui/PlanningSubpanels.ts`**: `SliderSheetPanel` (يُستخدم 3 مرات:
  إضاءة=شريط واحد، توهج=شريط واحد، مختبر=4 أشرطة) و`CameraPickerPanel`
  (زرا اختيار).

**تحقق بصري كامل:** فتحت لوحة المختبر (4 أشرطة ظهرت صحيحة)، سحبت شريط
الكثافة فعليًا من 150 إلى 500 (القيمة تحدّثت مباشرة)، فتحت لوحة اختيار
الكاميرا (الزران ظهرا جنبًا لجنب)، رفعت صورة تجريبية فعليًا عبر
`#mzj-bg-image` وتأكدت اللوحة توهّجت + رسالة التنبيه ظهرت + "صورة خلفية"
أصبحت نشطة.

**اختبار التسرب:** فتح لوحة، سحب شريط، لمس خلفية اللوحة، فتح لوحة الكاميرا،
لمس خلفيتها — انتظرت 2.5 ثانية، لا أي أثر صاروخ متسرب.

### الهوية الذهبية الموحدة (طلب إضافي لاحق، منجز الآن)
كل أيقونات العمود الـ14 (الرموز فقط، مو التسميات النصية — بقيت بيضاء كالتصميم
الأصلي) أصبحت `#fff6df` مع `AdvancedBloomFilter` حقيقي (استخراج السطوع +
تمويه + تركيب إضافي، نفس تقنية توهج الألعاب النارية في `FireworksSystem.ts`
واللوحات الجانبية في `SideDockPanel.ts`) و`DropShadowFilter` للعمق — لا
توهج مسطح ولا ظل CSS. تحقق بصري: لقطة مقرّبة تُظهر تدرجًا ذهبيًا حقيقيًا
وظلًا واضحًا تحت كل رمز، وعداد FPS بقي ٦٠ رغم 14 أيقونة × فلترين لكل واحدة —
لا أثر أداء ملحوظ.

### تفاصيل النافذة ٣ (`TextComposer.ts`، منجز الآن)
هندسة مُقاسة من CSS القديم (`#mzj-text-composer`/`.mzj-text-composer-*`)
قبل حذفه، مُحوّلة لصيغ Pixi دقيقة:
- **الشريط العلوي**: `COMPOSER_TOP_Y=68`، `COMPOSER_WIDTH=min(460, screen.width*0.94)`،
  زر رجوع دائري `BACK_DIAMETER=34` في أقصى اليمين (RTL)، حقل الإدخال يملأ
  الباقي (`GAP_BACK_INPUT=8`).
- **شريط التأثيرات**: توزيع `flex-wrap` القديم أُعيد بناؤه رياضيًا —
  `itemsPerRow = floor((composerWidth+14)/(78+14))` (تفاعلي مع عرض الشاشة،
  وليس ثابتًا 4+3)، كل صف يُحسب مركزه ثم تُوزَّع عناصره يمين→يسار
  (`TEXT_EFFECTS`'s الترتيب الأصلي: دخان، لهب، اللهب المتطور، نيون، خلل،
  شرار، بدون تأثير). إطار كل تأثير 78×44px بزوايا دائرية (`roundRect`
  نصف قطر 10)، حدوده الهوية الذهبية (`#fff6df`) مع `AdvancedBloomFilter`+
  `DropShadowFilter` حقيقيين — تحوّل شفافية بين خامل (0.22) ونشِط (0.9) بدل
  `border-color` مسطحة قديمة. التسميات تحت كل إطار بقيت بيضاء (نفس قاعدة
  "الأيقونة تتذهّب، التسمية تبقى بيضاء" من النافذة ٢).
- **زر الرجوع**: بقي بالمظهر الزجاجي البسيط (بلا توهج ذهبي) عمدًا — نفس
  قرار الهيدر: أزرار التنقّل ليست جزءًا من الهوية الذهبية، فقط المحتوى.
- **المعاينة المتسلسلة (Sequential Loop + Mask)**: `previewMask`
  و`previewReveal` كائن واحد مشترك يُعاد استخدامه لكل تأثير بالتتابع (لا
  7 أنظمة جزيئات متزامنة) — موضعه الآن يُحسب من `frame.root.getGlobalPosition()`
  بدل `getBoundingClientRect()` DOM القديمة. المستطيل المتحرك يقتصّ تمامًا
  عند حدود كل إطار مدوّر — تحقق بصري: صورة الدخان/اللهب لا تتجاوز الحدود
  المدوّرة أبدًا (انظر لقطة `zoom-smoke-frame.png`).
- **بروتوكول الجسر المخفي**: انظر القسم أعلاه.

**تحقق بصري كامل:** فتح المركّب عبر أيقونة "T"، كتابة نص عربي فعلي عبر
`page.keyboard.type()`، مشاهدة المؤشر الذهبي الوامض يتتبّع طول النص حيًا،
اختيار تأثير عبر لمس مباشر على الإطار وعبر لمس تسميته فقط (خارج الحدود
المرئية، داخل الـhitArea)، الرجوع لصندوق التحكم، السحب/التدوير/التكبير،
الإفلات خارج الصندوق للتثبيت.

**اختبار التسرب:** لمسات متكررة على حقل الإدخال، كل الأطر السبعة، وزر
الرجوع، ثم انتظار 2.5 ثانية — لا أثر صاروخ متسرب (بعد إصلاح الخلل رقم 4
أدناه).

## الأخطاء الحقيقية المكتشفة والمصححة حتى الآن
1. **الهيدر:** `stopPropagation()` على `pointertap` فقط لا يمنع تسرب اللمس
   لمستمع `pointerdown` في `app.stage`. أُصلح بإضافة `stopPropagation()` على
   `pointerdown` نفسه. طُبّق هذا الدرس فورًا في كل ملف لاحق (عمود الأيقونات،
   اللوحات الفرعية، شرائط التمرير) — صفر تكرار له منذ ذلك الحين.
2. **`UploadHint.ts`:** كان يستهدف `#mzj-glow` DOM بعد حذفه، فتعطّلت ميزة
   تمييز شريط التوهج بصمت (لا كراش، فقط لا تأثير). اكتُشف أثناء تدقيق CSS
   الميت (وليس بالصدفة)، أُصلح عبر ربط `UploadHint` بمرجع Pixi حقيقي
   (`SliderSheetPanel.pulse()`)، وتحقق بصري كامل بعد الإصلاح.
3. **ملاحظة معلّقة (لم تُصحَّح بعد، خارج نطاق هذه الجلسة):** نفس نمط
   الخطأ ١ (`stopPropagation` على `pointertap` فقط) موجود في
   `ShapesPanel.ts`/`SideDockPanel.ts` — كود قديم سابق لهذه الجلسة.
4. **`TextComposer.ts` — تسرب من الفجوات بين الأزرار (اكتُشف واصلح في
   النافذة ٣):** كل زر/إطار في المركّب يملك `hitArea` خاصة به فقط — لكن
   الفراغات *بينها* (فجوات الصفوف والأعمدة، المساحة تحت آخر صف) لم تكن
   مملوكة لأي كائن Pixi، فتسرّبت اللمسات مباشرة إلى مستمع إطلاق الصاروخ في
   `app.stage` تمامًا كما لو لم يكن المركّب موجودًا. عنصر `<div>` القديم
   كان يمتصّ كل لمسة داخل صندوقه تلقائيًا (سلوك DOM افتراضي)؛ لم يكن لهذا
   السلوك أي مكافئ تلقائي في Pixi. أُصلح بإضافة `catchAll: Graphics` —
   طبقة صامتة تغطي كامل صندوق المركّب (تُحسب أبعادها ديناميكيًا من
   `layoutEffectFrames()`)، تُضاف أولًا (خلف كل شيء بصريًا) فتفوز فقط في
   الفجوات الفارغة، بينما الأزرار الفعلية (تُضاف بعدها) تفوز دومًا داخل
   حدودها الخاصة. تحقق بصري: لمسة متعمدة في فجوة بين إطارين (344,208) —
   قبل الإصلاح أطلقت صاروخًا مرئيًا بوضوح، بعد الإصلاح: سماء نظيفة تمامًا.
   **درس عام يستحق المراجعة عبر بقية النوافذ المحوَّلة سابقًا**: أي حاوٍ
   Pixi يضم عدة أزرار متجاورة بفجوات بينها يحتاج فحصًا مماثلًا — لم يُراجَع
   هذا تحديدًا في `PlanningIconColumn.ts`/`PlanningSubpanels.ts` بعد (خطر
   محتمل، ليس مؤكدًا، لأن صفوف عمود الأيقونات هناك متباعدة عموديًا بفجوات
   أصغر ومرتبة في عمود واحد لا شبكة ثنائية الأبعاد — لكن يستحق تحققًا
   صريحًا لاحقًا).
5. **خلل هندسي دقيق (اكتُشف واصلح أثناء اختبار الـhitArea في النافذة ٣):**
   `hitArea` الأصلية لإطار التأثير كانت متمركزة حول مركز الصندوق المرئي
   بارتفاع متماثل (61px للأعلى وللأسفل من المركز)، رغم أن الصندوق المرئي
   نفسه (44px) يشغل النصف العلوي فقط والتسمية تحته — ما جعل حافة الـhitArea
   العلوية تتجاوز حدود الصندوق بمقدار 8.5px إضافية، فتتداخل مع حقل الإدخال
   فوقها (تداخل ~2.5px). أُصلح بجعل الـhitArea غير متماثلة (`FRAME_HIT_TOP`)
   تبدأ بالضبط من الحافة العلوية للصندوق المرئي وتمتد لأسفل عبر التسمية —
   صفر تداخل الآن (فجوة نظيفة 6px بين حقل الإدخال وأول صف).

## الخطوة التالية
النافذة ٤: `UploadHint`'s toast نفسه (`.mzj-upload-hint`)، ثم `#mzj-flash`.
مُقترَح إضافي (ليس ضمن الطلب الحالي، للمراجعة لاحقًا): تدقيق فجوات
الـhitArea بين الأزرار المتجاورة في `PlanningIconColumn.ts`/
`PlanningSubpanels.ts` بنفس منهجية الخطأ رقم 4 أعلاه.
