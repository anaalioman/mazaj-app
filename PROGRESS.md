# تطهير DOM/CSS → PixiJS خالص — سجل التقدم

آخر تحديث: بعد إنجاز النافذة ١ (الهيدر) والجزء الأول من النافذة ٢ (عمود
أيقونات شاشة التخطيط الـ14).

## الهدف
حذف `src/ui/mazajUI.css` وكل عنصر DOM يمكن تحويله فعليًا إلى كائنات PixiJS
(`Container`/`Graphics`/`Sprite`/`Text`) في منطقة عمل التطبيق، بنفس معايير
الدقة المطبّقة على صندوق تحكم النص: هندسة مُقاسة من DOM حقيقي (لا تخمين)،
مناطق لمس 44×44px صريحة، تحقق بصري بلقطات شاشة فعلية، واختبار عدم تسرب
اللمسات لمستمع "إطلاق الصاروخ" في `app.stage`.

## قيدان تقنيان حقيقيان — لن يختفيا مهما تحوّل الباقي
هذان ليسا تهربًا، بل استحالة تقنية موضوعية أوضحها بصراحة كاملة الآن قبل أي
عمل إضافي، بدل اكتشافها لاحقًا أو إخفائها:

1. **إدخال النص الحقيقي بالكيبورد** (`#mzj-text-composer-input` في
   `TextComposer.ts`) — لا يوجد أي مكافئ Pixi لحقل نص حقيقي (مؤشر كتابة،
   تحديد، لصق، دعم لوحة مفاتيح الجوال/IME). حتى Figma وCanva نفسهما
   (المرجعان اللذان استشهدت بهما) يستخدمان عنصر `<input>`/`contenteditable`
   حقيقي خلف الكواليس لأي إدخال نص فعلي — لا يوجد تطبيق واحد يرسم إدخال نص
   كامل يدويًا بكسل-ببكسل داخل Canvas.
2. **منتقي الملفات الأصلي من نظام التشغيل** (`#mzj-bg-image`/`#mzj-bg-video`
   في `PlanningScreen.ts`) — نافذة اختيار الملف/الفيديو من الجهاز نافذة
   نظام تشغيل (WebView chrome)، لا يمكن لأي كانفاس/WebGL استدعاءها إلا عبر
   عنصر `<input type="file">` حقيقي (حتى لو مخفيًا بصريًا). هذا قيد المتصفح
   نفسه، لا علاقة له بـPixi.

كل ما عدا هذين سيُحوَّل بالكامل. سأُبقي هذين العنصرين حصرًا (مخفيين حيث
أمكن، بلا أي تنسيق CSS مرئي إضافي)، وسأذكرهما بوضوح في كل تقرير حتى إغلاق
الملف.

## الجرد الكامل المتبقي (بعد النافذة ١)

| # | الموقع | العناصر | الحالة |
|---|--------|---------|--------|
| 1 | `HeaderBar.ts` | 4 أزرار (رئيسية/ابدأ العرض/FPS/كتم) | ✅ منجز — Pixi خالص |
| 2 | `PlanningScreen.ts` — العمود الجانبي | 14 زر أيقونة (`PlanningIconColumn.ts` جديد) | ✅ منجز — Pixi خالص، صفر DOM |
| 3 | `PlanningScreen.ts` — لوحة الكاميرا | زري اختيار (رفع/توثيق مباشر) | ⏳ لم يبدأ — لا يزال HTML، المحفّز (أيقونة الكاميرا) أصبح Pixi بالفعل |
| 4 | `PlanningScreen.ts` — لوحات الإضاءة/التوهج/المختبر | 6 أشرطة تمرير (sliders) | ⏳ لم يبدأ — يتطلب بناء عنصر Slider مخصص بالكامل في Pixi (لا يوجد حاليًا)؛ المحفّزات الثلاثة (أيقونات الإضاءة/التوهج/المختبر) أصبحت Pixi بالفعل |
| 5 | `UploadHint.ts` | تنبيه Toast عائم | ⏳ لم يبدأ |
| 6 | `TextComposer.ts` — الشريط العلوي | حقل الإدخال + شريط التأثيرات | ⚠️ حقل الإدخال = استثناء دائم (قيد ١)؛ شريط التأثيرات نفسه (الأزرار) قابل للتحويل |
| 7 | `#mzj-bg-image` / `#mzj-bg-video` | منتقيا ملفات مخفيان | ⚠️ استثناء دائم (قيد ٢) — المحفّز ("صورة خلفية") أصبح Pixi، يستدعي هذا الإدخال المخفي فقط |
| 8 | `#mzj-flash` | فلاش الكاميرا الأبيض | ⏳ لم يبدأ (بسيط — تعتيم كامل الشاشة) |

### تفاصيل البند ٢ (منجز الآن)
`src/ui/PlanningIconColumn.ts` (ملف جديد) — 14 صفًا (إضاءة الخلفية، العرض
التلقائي، المختبر، نص "T"، توليد عشوائي هجين، إطلاق جماعي، إطلاق متتابع،
الأشكال، فيديو خلفية حي، تسجيل فيديو، لقطة، صورة خلفية، توهج الألعاب النارية،
لون المقذوفة). هندسة مُقاسة حية (`getBoundingClientRect` على الـ14 عنصرًا
الأصليين قبل الحذف): `ROW_SPACING=58px`، أول صف مركزه Y=106.5 (صيغة:
`PADDING_TOP + (availableHeight - contentHeight)/2 + ROW_HEIGHT/2`، تحققت
رياضيًا تُطابق القياس تمامًا)، كل الصفوف بنفس المركز الأفقي (`centerX =
screen.width - 56`، مُقاس لا مُخمَّن). منطقة لمس 44×64px صريحة لكل صف (أعرض
من المقابض السابقة عمدًا لاستيعاب تفاوت عرض النصوص). زر التسجيل له هالة حمراء
نابضة حقيقية (طبقات دوائر متدرجة الشفافية + `blendMode:'add'`، نفس أسلوب
هالة التوهج في صندوق النص) بدل `@keyframes` من CSS. `getLeftEdgeX()` تحسب
حد الإرساء لـ`ColorPickerPanel`/`ShapesPanel` ديناميكيًا من أعرض صف فعلي حاليًا
(لا رقم ثابت مفترض) — تحقق بصري يؤكد الإرساء لا يزال دقيقًا بلا تراكب.

**خطأ أعيد اختباره ولم يتكرر:** طبّقت `stopPropagation()` على `pointerdown`
منذ اللحظة الأولى لهذا الملف (الدرس من خطأ الهيدر)، واختبرت 5 صفوف مختلفة
لا تُطلق شيئًا (العرض التلقائي، عشوائي، لقطة، صورة خلفية، توهج) — صفر تسرب.

## الأخطاء الحقيقية المكتشفة والمصححة حتى الآن
- **الهيدر:** `stopPropagation()` على `pointertap` فقط لا يمنع تسرب اللمس
  لمستمع `pointerdown` في `app.stage` (يُطلق صاروخًا تحت الزر). تم اكتشافه
  باختبار حي (5 لمسات على زر الكتم قبل/بعد) وتصحيحه بإضافة
  `stopPropagation()` على `pointerdown` نفسه أيضًا.
- **ملاحظة معلّقة:** نفس هذا النمط الخاطئ موجود في `ShapesPanel.ts` و
  `SideDockPanel.ts` (كود قديم سابق لهذه الجلسة) — لم يُصحَّح بعد، مُدرَج هنا
  ليُعالَج ضمن الجرد الشامل.

## الخطوة التالية
بناء عنصر Slider مخصص بالكامل في Pixi (البند ٤ — لا يوجد حاليًا في أي مكان
بالمشروع، أعقد قطعة متبقية)، ثم تحويل لوحات الإضاءة/التوهج/المختبر (6
أشرطة) ولوحة الكاميرا (زرا الاختيار) لاستخدامه — البند ٣+٤ معًا.
